module Main where

import           Test.QuickCheck
import           Test.QuickCheck.Checkers


-- main :: IO ()
-- main = do
--   let trigger = undefned :: IT (Int, Int, [Int])
--   quickBatch (traversable trigger)
